---
import Layout from '@/layouts/Layout.astro'
import { Bug, Home, RefreshCw } from 'lucide-react'
---

<Layout title='404 - Page not found'>
	<main class='flex min-h-[500px] flex-col items-center justify-center p-4'>
		<section class='space-y-6 text-center'>
			<h1 class='font-GilroyBold text-6xl font-bold text-primary'>404</h1>

			<div
				class='mb-4 flex items-center justify-center space-x-2 font-GilroySemibold text-4xl font-semibold'
			>
				<Bug className='h-8 w-8 text-[#ef4444]' />
				<span>¡Ups! Parece que hay un bug en la matriz</span>
			</div>

			<p class='mb-8 font-GilroyRegular text-xl'>
				La página que buscas se ha perdido en el void pointer.
			</p>

			<div
				class='flex flex-col items-center justify-center space-y-4 sm:flex-row sm:space-x-4 sm:space-y-0'
			>
				<a
					href='/'
					class='flex items-center space-x-2 bg-primary px-4 py-2 font-GilroySemibold font-semibold text-primary-foreground transition-colors hover:bg-primary/90'
				>
					<Home className='h-4 w-4' />
					<span>Volver al inicio</span>
				</a>
				<button
					class='flex items-center space-x-2 bg-secondary px-4 py-2 font-GilroySemibold font-semibold text-secondary-foreground transition-colors hover:bg-secondary/90'
					id='reload-button'
				>
					<RefreshCw className='h-4 w-4' />
					<span>Recargar página</span>
				</button>
			</div>

			<div class='mt-12 rounded-lg bg-black px-4 py-3 font-mono text-neutral-200 shadow-inner'>
				<p class='text-[#ff5e69]'>&gt; Error 404: Page not found</p>
				<p class='text-[#b16cea]'>&gt; Running diagnostics...</p>
				<p class='text-[#ffa84b]'>&gt; Suggestion: Check the URL or navigate back to safety</p>
			</div>
		</section>
	</main>
</Layout>

<script>
	document.getElementById('reload-button').addEventListener('click', (e) => {
		e.preventDefault()
		window.location.reload()
	})
</script>
