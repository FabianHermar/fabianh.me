---
import { sanitizeHtml } from '@/helpers/satinizeHTML'
import { getI18N } from '@/i18n'

const { currentLocale } = Astro
const i18n = getI18N({ currentLocale })
---

<div class='prose max-w-full px-3 pb-4 pt-4 md:pb-0'>
	<p class='font-GilroyRegular text-black' set:html={sanitizeHtml(i18n.hero.bento.hobbies.fp)} />
	<p class='font-GilroyRegular text-black' set:html={sanitizeHtml(i18n.hero.bento.hobbies.sp)} />
</div>
