---
import Layout from '@/layouts/Layout.astro'
import { Bug, Home, RefreshCw } from 'lucide-react'
import Header from '@/components/Header.astro'
---

<Layout title='404 - Page not found'>
	<Header />
	<main class="flex flex-col items-center justify-center min-h-[500px] p-4">
		<section class="text-center space-y-6">
			<h1 class="text-6xl font-bold font-GilroyBold text-primary">
				404
			</h1>

			<div class="text-4xl font-semibold font-GilroySemibold mb-4 flex items-center justify-center space-x-2">
				<Bug className="w-8 h-8 text-[#ef4444]" />
				<span>¡Ups! Parece que hay un bug en la matriz</span>
			</div>

			<p class="text-xl mb-8 font-GilroyRegular">
				La página que buscas se ha perdido en el void pointer.
			</p>

			<div class="flex flex-col sm:flex-row justify-center items-center space-y-4 sm:space-y-0 sm:space-x-4">
				<a
					href="/"
					class="flex items-center font-GilroySemibold font-semibold space-x-2 px-4 py-2 bg-primary text-primary-foreground hover:bg-primary/90 transition-colors"
				>
					<Home className="w-4 h-4" />
					<span>Volver al inicio</span>
				</a>
				<button
					class="flex items-center font-GilroySemibold font-semibold space-x-2 px-4 py-2 bg-secondary text-secondary-foreground hover:bg-secondary/90 transition-colors"
					id="reload-button"
				>
					<RefreshCw className="w-4 h-4" />
					<span>Recargar página</span>
				</button>
			</div>

			<div class="mt-12 px-4 py-3 bg-black text-neutral-200 rounded-lg shadow-inner font-mono">
				<p class="text-[#ff5e69]">&gt; Error 404: Page not found</p>
				<p class="text-[#b16cea]">&gt; Running diagnostics...</p>
				<p class="text-[#ffa84b]">&gt; Suggestion: Check the URL or navigate back to safety</p>
			</div>
		</section>
	</main>
</Layout>

<script>
	document.getElementById('reload-button').addEventListener('click', (e) => {
		e.preventDefault();
		window.location.reload();
	});
</script>